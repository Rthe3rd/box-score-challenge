// 
const BSCController = require("../controllers/bsc.controller")

module.exports = app => {
    // app.get() get request -> retrieve data from db
    // app.post() post request -> write data to db
    // app.put() put request -> update data in db
}